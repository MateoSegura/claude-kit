{
  "name": "core-spec",
  "description": "Spec-driven development for long-running Claude Code sessions — write the spec first, derive implementation phases from it, track progress via file-based state, and recover context after compaction through hooks and agents.",
  "version": "1.0.0"
}
