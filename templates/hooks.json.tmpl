{
  "hooks": {
    "PreToolUse": [
      {
        "matcher": "{{TOOL_MATCHER}}",
        "hooks": [
          {
            "type": "command",
            "command": "${CLAUDE_PLUGIN_ROOT}/scripts/{{SCRIPT_NAME}}"
          }
        ]
      }
    ]
  }
}
